pipeline {
    agent any
    stages {
         stage ('code checkout') {
            steps {
                git url: 
                echo "code checkout completed"
            }
            
         }
    }
}